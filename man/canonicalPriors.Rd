% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior-thread.R
\name{canonicalPriors}
\alias{canonicalPriors}
\title{Build Canonical Prior Table}
\usage{
canonicalPriors(
  object,
  method = c("ices", "fishlife", "flbrp"),
  source = NULL,
  priorCv = 0.3,
  ...
)
}
\arguments{
\item{object}{An `FLStock` or a JABBA prior object with `pr` and `pr.sd`.}

\item{method}{Prior pathway used when `object` is `FLStock`.}

\item{source}{Label stored in canonical output.}

\item{priorCv}{Default coefficient of variation used by `jabbaPriors()`.}

\item{...}{Additional arguments forwarded to `jabbaPriors()` when relevant.}
}
\value{
A data.frame with columns `param`, `mean`, `sd`, `cv`, `source`.
}
\description{
Build a canonical prior table (`param`, `mean`, `sd`, `cv`, `source`) from
either an `FLStock` plus `jabbaPriors()` method, or an existing JABBA prior
object (`list(pr, pr.sd)`).
}
