% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{ageMax}
\alias{ageMax}
\alias{ageMax,FLQuant-method}
\title{Find Age with Maximum Value}
\usage{
ageMax(object, na.rm = TRUE, ...)

\S4method{ageMax}{FLQuant}(object, na.rm = TRUE, ...)
}
\arguments{
\item{object}{An FLQuant object with age, year, and iter dimensions}

\item{na.rm}{Logical. Should NA values be removed? Default is TRUE}

\item{...}{Additional arguments (not currently used)}
}
\value{
An FLQuant object with 1 quant dimension, containing the age indices
where the maximum value occurs for each year and iteration
}
\description{
Identifies which age has the highest value for each year and iteration in an FLQuant object.
Excludes the plusgroup (last age) from the calculation.
}
\examples{
\dontrun{
# Example with sample data
data(ple4)
max_age = ageMax(ssb(ple4))

# Get the actual ages (not indices)
ages = an(dimnames(ple4)$age)
max_age_actual = ages[c(max_age)]
}

}
