% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior-thread.R
\name{comparePriorApproaches}
\alias{comparePriorApproaches}
\title{Compare Prior Approaches for a Stock}
\usage{
comparePriorApproaches(
  object,
  methods = c("ices", "fishlife", "flbrp"),
  priorCv = 0.3,
  strict = TRUE,
  ...
)
}
\arguments{
\item{object}{An `FLStock`.}

\item{methods}{Character vector of methods to compare.}

\item{priorCv}{Default coefficient of variation passed to `jabbaPriors()`.}

\item{strict}{If `TRUE`, stop on the first method error. If `FALSE`, keep
successful methods and attach failures in `failures`.}

\item{...}{Additional arguments forwarded to `jabbaPriors()`.}
}
\value{
A list with:
\describe{
  \item{canonicalByMethod}{Named list of canonical prior data.frames.}
  \item{comparison}{Single combined comparison table.}
  \item{failures}{Named list of error messages for failed methods (if any).}
}
}
\description{
Build and compare canonical priors across multiple `jabbaPriors()` methods
in a single call.
}
