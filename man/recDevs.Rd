% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recDevs.R
\name{recDevs}
\alias{recDevs}
\alias{recDevs,FLSR-method}
\alias{recDevs,FLSRs-method}
\alias{recDevs,FLBRP-method}
\alias{recDevs,FLBRPs-method}
\title{Recruitment residuals and regime mean / SD}
\usage{
recDevs(object, ...)

\S4method{recDevs}{FLSR}(object, scale = "log", nits = 0L, ...)

\S4method{recDevs}{FLSRs}(object, scale = "log", nits = 0L, ...)

\S4method{recDevs}{FLBRP}(object, scale = "log", nits = 0L, pe = FALSE, stock = NULL, ...)

\S4method{recDevs}{FLBRPs}(object, scale = "log", nits = 0L, pe = FALSE, stock = NULL, ...)
}
\arguments{
\item{object}{An \code{FLSR}, \code{FLSRs}, \code{FLBRP} or \code{FLBRPs}.}

\item{...}{Passed to \code{\link{rod}} (\code{n}, \code{sig}, \ldots).}

\item{scale}{Residual scale: \code{"log"} (default), \code{"mult"}
(\eqn{R/\hat{R}}), or \code{"raw"} (\eqn{R-\hat{R}}).}

\item{nits}{Integer. If \code{> 0}, simulate that many iterations of
regime-scaled recruitment deviations.}

\item{pe}{Logical. For \code{FLBRP} / \code{FLBRPs}, also return surplus-
production process error and its \code{rod()} table.}

\item{stock}{Optional \code{FLStock} / \code{FLStocks} used if
\code{processError(FLBRP)} fails and \code{pe = TRUE}.}
}
\value{
A named list:
\describe{
  \item{residuals}{\code{FLQuant} or \code{FLQuants} of residuals}
  \item{mn}{Regime mean by year (\code{FLQuant} / \code{FLQuants})}
  \item{sd}{Regime SD by year (\code{FLQuant} / \code{FLQuants})}
  \item{rod}{\code{data.frame} of regime polygons (\code{sid} on collections)}
  \item{sim}{Simulated deviations if \code{nits > 0}, else \code{NULL}}
  \item{pe, pe_rod}{If \code{pe = TRUE}}
}
}
\description{
Extracts stock–recruit residuals and expands STARS regime mean and SD
(\code{\link{rod}}, \code{\link{rodMn}}, \code{\link{rodSD}}) to each
observation. The default scale is log (FLSR residuals). Optional
simulated multiplicative deviations follow the 02.0 construction:
\code{exp(log(rlnoise(nits, rsdl \%=\% 0)) \%*\% rodSD + rodMn)}.
}
\examples{
\dontrun{
data(nsher, package = "FLCore")
recDevs(nsher)
recDevs(nsher, scale = "mult", nits = 20)
}
}
\seealso{
\code{\link{peDevs}}, \code{\link{rod}}, \code{\link{rodMn}},
  \code{\link{rodSD}}, \code{\link{processError}}
}
