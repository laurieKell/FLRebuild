% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPe.R
\name{plotPe}
\alias{plotPe}
\title{Four-Panel Process-Error Diagnostics}
\usage{
plotPe(
  object,
  pe = "pe",
  status = "bbmsy",
  lag.max = 10,
  bins = 40,
  regimes = TRUE,
  n = 10,
  sig = 1.68,
  combine = TRUE,
  ...
)
}
\arguments{
\item{object}{\code{FLQuants} with at least \code{pe} (preferably also
\code{bbmsy}); or an \code{FLQuant} of residuals; or a JABBA fit
(passed through \code{\link{jabbaTs}}).}

\item{pe}{Name of the process-error series. Default \code{"pe"}.}

\item{status}{Name of the relative biomass series for panel 2.
Default \code{"bbmsy"}; falls back to \code{"stock"}.}

\item{lag.max}{Maximum ACF lag (default 10).}

\item{bins}{Histogram bins (default 40).}

\item{regimes}{Overlay STARS polygons via \code{\link{rod}} (default TRUE).}

\item{n, sig}{Arguments forwarded to \code{\link{rod}}.}

\item{combine}{If TRUE and \pkg{patchwork} is available, return a 2x2
combined plot; otherwise a named list of ggplots.}

\item{...}{Unused.}
}
\value{
A patchwork object, or a named list with elements
\code{time}, \code{status}, \code{acf}, \code{hist}.
}
\description{
Process-error residual diagnostics: (1) residuals vs time with optional
STARS regime polygons, (2) residuals vs relative biomass, (3) ACF,
(4) residual histogram with normal density overlay.
}
\examples{
\dontrun{
plotPe(jabbaTs(fit))
}

}
\seealso{
\code{\link{jabbaTs}}, \code{\link{rod}}, \code{\link{jabbaPE}}
}
