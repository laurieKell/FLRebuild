% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generic.R, R/biodyn-rebuild.R,
%   R/rebuild-methods.R
\name{rebuildTime}
\alias{rebuildTime}
\alias{rebuildTime,FLStock-method}
\alias{rebuildTime,biodyn-method}
\title{Calculate rebuilding time}
\usage{
rebuildTime(
  object,
  target = NULL,
  nInitial = 100,
  growthRate = 0.25,
  minVal = 1e-06,
  maxVal = 1,
  burnin = 20,
  truncate = TRUE,
  ...
)

rebuildTime(
  object,
  target = NULL,
  nInitial = 100,
  growthRate = 0.25,
  minVal = 1e-06,
  maxVal = 1,
  burnin = 20,
  truncate = TRUE,
  ...
)

\S4method{rebuildTime}{FLStock}(object)

\S4method{rebuildTime}{biodyn}(
  object,
  target = refpts(object)["bmsy"],
  nInitial = 100,
  growthRate = 0.3,
  minVal = 1e-06,
  maxVal = 1,
  nx = 101
)
}
\arguments{
\item{object}{A biodyn object}

\item{target}{Target biomass (default = BMSY)}

\item{nInitial}{Number of initial depletion levels (default = 100)}

\item{growthRate}{Growth rate for depletion sequence (default = 0.3)}

\item{minVal}{Minimum depletion value (default = 1e-6)}

\item{maxVal}{Maximum depletion value (default = 1)}

\item{...}{Additional arguments}

\item{nx}{Number of interpolation points (default = 101)}
}
\value{
A data frame with columns year and initial

A data frame with columns:
  \item{year}{Projection year}
  \item{initial}{Initial depletion level relative to BMSY}
}
\description{
Projects stock rebuilding trajectories from different initial depletion levels
}
\examples{
bd=biodyn(FLPar(r=0.5, k=1000, p=1))
rebuild_data=rebuild(bd)
}
